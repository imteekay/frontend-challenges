import React from 'react';
import Tooltip from '../../../components/Tooltip';
import PriceSummary from './PriceSummary';

const PriceSummaryTooltip = () => (
  <Tooltip>
    <PriceSummary />
  </Tooltip>
);

export default PriceSummaryTooltip;
